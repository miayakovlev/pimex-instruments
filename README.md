# SPIMEX Instruments Export

Небольшой набор скриптов для **автоматической выгрузки данных** с публичных карточек инструментов **рынка нефтепродуктов** Санкт-Петербургской международной товарно-сырьевой биржи ([spimex.com](https://spimex.com/)): по списку кодов или полных ссылок вида `detail.php?code=...` формируется **единый CSV**, при необходимости файл **пересылается по электронной почте** (SMTP). На сервере Linux расписание задаётся через **systemd timer** (раз в сутки в **13:20 по Москве**) или через **cron**.

Подробная установка и эксплуатация: **[INSTALL.md](INSTALL.md)**.

## Задача

Вручную собирать из карточки инструмента:

- полное наименование по полю **«Биржевой товар»**;
- текст по полю **«Базис поставки»**;
- для таблицы **«Результаты последних 10 торговых сессий»** — значение колонки **«Последняя»** по **самой поздней дате** торгов.

Скрипт повторяет это для каждой строки списка и записывает результат в таблицу.

## Выходные данные

Файл **CSV в кодировке UTF-8 с BOM** (удобно открывать в Excel): столбцы

| Поле CSV | Смысл |
|----------|--------|
| `code` | Код инструмента |
| `url` | Полная ссылка на карточку |
| `birzhevoi_tovar` | Биржевой товар (полное текстовое наименование) |
| `bazis_postavki` | Базис поставки |
| `data_posledney_sessii` | Дата строки последней торговой сессии из таблицы (ДД.MM.ГГГГ) |
| `tsena_poslednyaya_rub_posledniaia` | Колонка «Последняя» для этой даты |

Имя файла по умолчанию: `spimex_instruments.csv` (перезаписывается при каждом запуске при успешном проходе).

## Состав проекта

| Файл / каталог | Назначение |
|----------------|------------|
| `spimex_export.py` | Основная логика: HTTP, парсинг HTML, запись CSV, опционально SMTP |
| `requirements.txt` | Зависимости Python (`requests`, `beautifulsoup4`) |
| `urls.txt` | Список URL или кодов инструментов (пример; правьте под себя) |
| `run_spimex_daily.sh` | Оболочка: подхватывает `.env`, вызывает `spimex_export.py` |
| `install_background.sh` | Однократная установка **systemd timer** (13:20 МСК) |
| `systemd/` | Шаблоны unit-файлов для генерации при установке |
| `env.example`, `env.rambler.example` | Примеры переменных окружения для почты и путей |
| `cron.moscow.example` | Пример строки cron, если не используете systemd |

Файлы **`.env`** (пароли SMTP) и **`.venv/`** не коммитятся — см. `.gitignore`.

## Быстрый ручной запуск

Из каталога проекта после создания виртуального окружения (см. [INSTALL.md](INSTALL.md)):

```bash
.venv/bin/python spimex_export.py -f urls.txt -o spimex_instruments.csv
```

Отправить CSV после выгрузки (получатель также может быть из `.env`: `SPIMEX_MAIL_TO`):

```bash
set -a && source .env && set +a
.venv/bin/python spimex_export.py -f urls.txt -o spimex_instruments.csv --mail-to получатель@example.com
```

Полный список ключей командной строки (задержка между запросами, браузер Playwright как запасной вариант и т.д.) описаны внутри скрипта: `python spimex_export.py --help`.

## Расписание на сервере

Рекомендуемый вариант: **`./install_background.sh`** один раз ставит systemd timer по **Europe/Moscow, 13:20**. Вариант без systemd — **`cron.moscow.example`**.

Таймер каждый день выполняет `run_spimex_daily.sh`, который читает `urls.txt`, обновляет CSV и отправляет почту при настроенных переменных в `.env`.

## Ограничения и юридическая сторона

Данные публикуются на сайте биржи, но режим их использования (в том числе автоматизированное скачивание и дальнейшая передача) регулируется **пользовательским соглашением** и при необходимости **договорами об информационных услугах SPIMEX**. Перед промышленной нагрузкой и передачей третьим лицам стоит это учесть. Проект задуман как вспомогательный инструмент для внутреннего офисного процесса.

## Обратная связь по работе почты и сайта

- Почтовый сервер должен поддерживать **STARTTLS** (порт **587**) или **SMTPS** (порт **465**). Для 465 используйте `SPIMEX_SMTP_USE_SSL=1` в `.env` (шаблон Рамблер в `env.rambler.example`).
- Если вёрстка spimex.com изменится или часть блоков начнёт подгружаться только через JavaScript, может понадобиться режим **`--browser-fallback`** и установка **Playwright** (редко нужно для текущих страниц карточек).

